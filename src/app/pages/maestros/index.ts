import { Routes } from '@angular/router';
import { FormulariosComponent } from './formularios/formularios.component'

export const Maestros : Routes = [
    {
        path:'formularios',
        component : FormulariosComponent
    }
]